# Gemini API Configuration
# Get your API key from: https://ai.google.dev/gemini-api/docs/api-key
GEMINI_API_KEY=your_api_key_here

# GitHub Personal Access Token - REQUIRED for API rate limits
# Increases rate limit from 60/hour to 5000/hour
# Create at: https://github.com/settings/tokens/new (no scopes needed for public repos)
GITHUB_TOKEN=your_github_token_here

# Redis Storage - REQUIRED for Vercel deployment
# Automatically set by Vercel when you add Upstash Redis from Storage tab
# For local development, leave empty (will use filesystem instead)
REDIS_URL=
